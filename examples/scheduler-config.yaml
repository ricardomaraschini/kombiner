apiVersion: kubescheduler.config.k8s.io/v1
kind: KubeSchedulerConfiguration
clientConnection:
  kubeconfig: /path/to/.kube/config
leaderElection:
  resourceName: kombiner-scheduler
profiles:
- schedulerName: kombiner-scheduler
  plugins:
    bind:
      enabled:
      - name: PlacementRequestBinder
      disabled:
      - name: "*"
  pluginConfig:
  - name: PlacementRequestBinder
    args: {}
